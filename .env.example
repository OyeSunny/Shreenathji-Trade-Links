POSTGRES_PASSWORD=change-me-local-only
DATABASE_URL=postgresql://postgres:change-me-local-only@127.0.0.1:5432/shreenathji_trade_links?schema=public
BETTER_AUTH_SECRET=replace-with-a-random-32-plus-character-secret
BETTER_AUTH_URL=http://localhost:3000
OWNER_EMAIL=owner@example.com
OWNER_SETUP_TOKEN=replace-with-a-one-time-random-setup-token
MAIL_FROM=no-reply@example.com
# Optional: a separate inbox for new quote requests and consented contact leads.
# When omitted, notifications are delivered to MAIL_FROM.
CONTACT_NOTIFICATION_TO=owner@example.com
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=owner@example.com
SMTP_PASSWORD=replace-with-your-smtp-app-password
