# These values belong only on the VPS at /opt/shreenathji-analytics/.env.
# Generate both secret values on the VPS. Never commit a filled-in copy.
UMAMI_DB_NAME=umami
UMAMI_DB_USER=umami
UMAMI_DB_PASSWORD=replace-with-a-random-password
UMAMI_APP_SECRET=replace-with-a-random-secret
